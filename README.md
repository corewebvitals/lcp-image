# lcp-image — moved

This experiment now lives in the consolidated experiments hub:

- **Live demo:** https://corewebvitals.github.io/pagespeed/experiments/lcp-image/
- **Source:** https://github.com/corewebvitals/pagespeed/tree/main/experiments/lcp-image

The full git history of this repo was imported via `git subtree` and is preserved in `pagespeed`.

This repo is archived. Existing URLs under `corewebvitals.github.io/lcp-image/...` keep working — they redirect to the new path.
